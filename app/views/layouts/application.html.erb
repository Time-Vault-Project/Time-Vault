<!DOCTYPE html>
<html>
  <head>
    <title>TimeVault</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body class="min-vh-100 d-flex flex-column justify-content-between">
  <div class="container">
    <header class="blog-header py-3">
      <div class="row flex-nowrap justify-content-between align-items-center">
        <div class="col-4 pt-1">
          <%= link_to 'Time Vault', root_url, class: 'link-secondary' %>
        </div>
        <div class="col-4 text-center">
          <%= link_to controller_name.singularize.capitalize, controller_path, class: 'blog-header-logo text-dark' %>
        </div>
        <div class="col-4 d-flex justify-content-end align-items-center">
          <a class="link-secondary" href="#" aria-label="Search">
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" class="mx-3" role="img" viewBox="0 0 24 24"><title>Search</title><circle cx="10.5" cy="10.5" r="7.5"/><path d="M21 21l-5.2-5.2"/></svg>
          </a>
          <% if user_signed_in? %>
            <%= link_to 'Sign out', destroy_session_path(current_user), data: { turbo_method: :delete }, class: 'btn btn-sm btn-outline-secondary' %>
          <% end %>
        </div>
      </div>
    </header>
  </div>
  <%= yield %>

  <footer class="blog-footer">
    <p>We are what we remember</p>
  </footer>
  </body>
</html>
